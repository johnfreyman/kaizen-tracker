
  # Kaizen Tracker

  Kaizen Tracker is a team attendance and training-hours tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
